aside.control-panel
  input#searchInput.form-control.mb-3(
    type="text",
    placeholder="filter (i.e. food,art)",
    data-bind="event: { input: searchInputChange }")
  ul.list-group(data-bind="foreach: visableListings")
   li.list-group-item(data-bind="text: title")
